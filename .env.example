# Copy to `.env.local` for local secrets if needed later.
# This site currently has no required environment variables for build/runtime.
# Vercel: Project Settings → Environment Variables (add only when you connect a CRM/API).
